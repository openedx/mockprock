[tox]
envlist = py312, lint
requires = tox-uv>=1

[testenv:lint]
runner = uv-venv-lock-runner
dependency_groups = quality
commands =
    ruff check .
    ruff format --check .

[testenv]
runner = uv-venv-lock-runner
dependency_groups = test
commands = pytest {posargs}
