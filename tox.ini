[tox]
envlist = py311-test, py312-test, lint
requires = tox-uv>=1

[testenv:lint]
runner = uv-venv-lock-runner
dependency_groups = quality
commands =
    ruff check .
    ruff format --check .

[testenv]
runner = uv-venv-lock-runner
dependency_groups = test
commands = pytest {posargs}
